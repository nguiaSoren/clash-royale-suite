[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "cr_engine"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
manifest-path = "engine/Cargo.toml"
